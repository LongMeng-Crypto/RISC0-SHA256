[package]
name = "daa_sha256_backend_methods"
version = "0.1.0"
edition = "2021"
build = "build.rs"

[build-dependencies]
risc0-build = { version = "^3.0.4" }

[package.metadata.risc0]
methods = ["guest"]
